\# 实验03：TIM2定时器中断控制LED闪烁

\## 实验目的

掌握STM32通用定时器的配置与中断机制，实现非阻塞式精准延时，替代传统阻塞式延时函数。

\## 硬件平台

\- 主控芯片：STM32F411RET6

\- 外设：板载LED（PA5）、TIM2通用定时器

\## 核心实现

配置系统时钟为100MHz，TIM2定时器10ms触发一次中断，通过中断计数累计50次（500ms）翻转一次LED电平，主循环空转，所有逻辑由中断异步完成。

\## 实验现象

板载LED以500ms亮、500ms灭的周期稳定闪烁，定时精准无阻塞。

